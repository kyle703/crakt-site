export default function Banner() {
  return (
    <div className="fixed top-0 left-0 right-0 z-[200] bg-accent text-cream text-center py-2 px-4 font-head text-[0.9rem] font-bold tracking-[0.12em] uppercase">
      🔔 Coming Soon &nbsp;
      <span className="opacity-85 font-normal tracking-[0.06em] normal-case">
        Crakt is in pre-release — sign up to be notified at launch
      </span>
    </div>
  )
}
